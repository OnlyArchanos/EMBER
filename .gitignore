# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# === Environment ===
.env
backend/.env

# === SQLite databases ===
*.db
*.sqlite3

# === Runtime data directories (seed/ is committed, raw/ and processed/ are not) ===
data/raw/
data/processed/
backend/data/raw/
backend/data/processed/

# === ML artifacts ===
# The trained model file(s) under app/ml/artifacts/ are COMMITTED on purpose
# (see RULES.md §9) — a fresh checkout must work offline without retraining
# under demo-day time pressure, same reasoning as data/seed/ being committed.
# Deliberately nothing ignored in this directory.

# === Node / Frontend ===
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/

# === IDE / OS ===
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# === Logs ===
*.log