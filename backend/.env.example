# All variable names must match what backend/app/config.py reads exactly.
# Replace placeholder values with real ones in your local backend/.env (never commit that file).

NASA_FIRMS_MAP_KEY=your_firms_map_key_here
NOMINATIM_USER_AGENT=EMBER-fire-detection/1.0 contact@example.com
DATABASE_URL=sqlite:///./backend/data/sih_fire.db
DATA_MODE=seed
FETCH_INTERVAL_HOURS=24
LOG_LEVEL=INFO
